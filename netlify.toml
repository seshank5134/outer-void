[build]
  base = "frontend"
  command = "[ -d flutter-sdk ] || git clone https://github.com/flutter/flutter.git --depth 1 -b stable flutter-sdk && export PATH=\"$PATH:`pwd`/flutter-sdk/bin\" && flutter config --enable-web && flutter build web --release"
  publish = "build/web"
